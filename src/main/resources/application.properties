#MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=root
spring.datasource.password=${MYSQL_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#set context path
server.servlet.context-path=/api/v1

#JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

##Email Configuration
#spring.mail.host=smtp-relay.brevo.com
#spring.mail.port=587
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.protocol=smtp
## ADD THIS LINE: Explicitly trust the SMTP host for TLS connections
#spring.mail.properties.mail.smtp.ssl.trust=smtp-relay.brevo.com





# --- Email Configuration (USING GMAIL SMTP - PORT 465) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME}
spring.mail.password=${GMAIL_PASSWORD}

# NO spring.mail.protocol=smtp line here

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.from=${GMAIL_FROM_EMAIL}
# --------------------------------------------------------

#JWT
jwt.secret=2b8e7c7e-4e2a-4b1a-9cze-8f7e2d3c4b5a

#frontend url
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}

app.activation.url=${MONEY_MANAGER_BACKEND_URL}

spring.profiles.active=prod

# Add this line to the bottom section
spring.datasource.driver-class-name=org.postgresql.Driver